
cc.Class({
  extends: cc.Component,

  properties: {
  },

  // LIFE-CYCLE CALLBACKS:

  // onLoad () {},

  init(type, info) {
    this.type = type;
    this.info = info;
  },
  // update (dt) {},
});
